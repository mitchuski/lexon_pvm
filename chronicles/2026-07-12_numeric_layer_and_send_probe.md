# 2026-07-12 · Plan steps 1 and 2: the send-probe and the numeric layer

## Verdict first

Both pre-lexr5 keystone extensions from CORPUS_COVERAGE_PLAN.md are built,
proven, and additive. relation selftest 12/12, base selftest 11/11, blocks
8/8, spells 13/13, coverage-debt unchanged at 64 with zero failed entries
(all 147 folded entries and all 13 constitution spells re-verified untouched
under the extended checker), CONFORM PASS clean. CR-21 and CR-22 filed as
PROVEN; CR-20 moves to mitigated. The gate lexr5 needs now exists before the
run that needs it, which is the rule this workstream keeps proving.

## Step 1 · The send-probe (CR-21, closes CR-20's mechanism)

The absence-claim twin used to pay its probe object, which forced recipes to
type every absence-guarded object as an amount (a secret, a mandate, and
Light were all amounts, solely for the checker). The probe now follows the
object's TYPE: amounts pay (from escrow where escrow context exists), texts
and data send, the remainder pseudo-object and the themselves-alias behave
as before. The relation fixture gained a text object (the Scroll) and two
new claims proving the send-probe both passes and catches, permanently in
the selftest. Folded entries keep their disclosed typing; the rule binds
future authoring.

## Step 2 · The numeric layer (CR-22)

Grounded production by production in the vendored 0.7 sample
(lexon-example-oversimplicated), per the standing rule that checker,
GRAMMAR.ebnf.md, and selftest move together:

- definition types `number` and `account`
- initial numeric literals: `"Maximum Members Count" is a number,
  initially 99.` (literals bind only to number and amount types)
- the `set` verb (`sets the Trader Rate`, recital-attested)
- `appoint` accepts accounts (`appoints the Maintenance Account`)
- comparative condition scaffold: `greater than zero`, `greater than or
  equal to half of`, decimals as bare literals in conditions

New golden (numeric.lex, constructed from the attested shapes, canary by
construction) and two new mutants (a missing comma in the initial literal;
an unbound comparand in a comparative) that must fail, and do.

**Deliberately excluded: percent literals.** The assay pipeline byte-scans
proposals for confidence percentages (the GR-3 hard constraint that has now
caught two mirages); a % in lexText would collide with that scan. Excluded
with the reason written into GRAMMAR.ebnf.md, revisitable only by operator
convention.

## What this unlocks

The formula-hard census residue (holographic bound and field, behavioural
density, path value, conditional independence, ZKP proper, the toroidal and
quaternion geometry terms) can now be expressed with quantity-bearing
anchors and comparative predicates instead of shape-only claims. That is
Phase 1.2 of the corpus plan delivered; Phase 1.3 (lexr5 under OT-7: merged
charter, round disjointness, the residue) is ready to run and is the
operator's call per the plan's sequencing table.

## Handoff

1. lexr5 awaits the word (plan step 3). Config amendments needed at launch:
   the merged-charter instruction and the disjointness binding (round-level,
   the lexr4.2 critic's rule).
2. Plan steps 4-7 (C-series convention, census closure, conjecture census,
   model census) are operator gates, unchanged.
3. Door stack unchanged: Diedrich brief, CTR-LEX triage, bonfires packet,
   canon propagation, skill and SPECIALISATION projections.
